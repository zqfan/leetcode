/*  Implement strStr().

    Returns a pointer to the first occurrence of needle in haystack,
    or null if needle is not part of haystack. */
#include <iostream>
using namespace std;

class Solution {
public:
  // kmp and suffix tree can be very efficient, however, they are
  // too complicated for a single method
  char *strStr(char *haystack, char *needle) {
  }
};

int main() {
  return 0;
}
